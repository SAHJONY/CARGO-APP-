{
  "name": "cargo-truck-app",
  "version": "0.1.0",
  "private": true,
  "description": "Worldwide direct freight marketplace",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
